% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_leptos_dynamic_items_input.R
\name{shiny_leptos_dynamic_items_input}
\alias{shiny_leptos_dynamic_items_input}
\title{Dynamic List Input Widget}
\usage{
shiny_leptos_dynamic_items_input(inputId, value = list(), width = NULL)
}
\arguments{
\item{inputId}{The \code{input} slot that will be used to access the value.}

\item{value}{Initial value for the list. Should be a character vector or list.
Defaults to an empty list.}

\item{width}{The width of the input, e.g. \code{'400px'}, or \code{'100\%'};
see \code{\link[shiny]{validateCssUnit}}.}
}
\value{
An HTML tag object that can be included in a Shiny UI definition.
}
\description{
Creates a dynamic list input widget using Leptos (Rust/WASM). Users can
add items using a text input and remove items using buttons.
}
\examples{
if (interactive()) {
  library(shiny)
  library(shiny.leptos.showcase)

  ui <- fluidPage(
    h3("Dynamic Items Input"),
    shiny_leptos_dynamic_items_input(
      "dynamicList",
      value = list("Initial Item 1", "Initial Item 2"),
      width = "400px"
    ),
    hr(),
    h4("Current Value:"),
    verbatimTextOutput("dynamicListOut")
  )

  server <- function(input, output, session) {
    output$dynamicListOut <- renderPrint({
      input$dynamicList
    })
  }

  shinyApp(ui, server)
}
}
